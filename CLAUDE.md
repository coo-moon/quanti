# CLAUDE.md

本文件是 Claude Code 在本项目（quanti，A 股量化交易系统）工作时**必须遵守**的项目规则。

## 工作流程

1. **需求先做可行性评估（ultracode 模式）**
   用户提出需求后，先用 ultracode 模式系统性评估其可行性，再动手实现。评估维度包括但不限于：
   - **能否真正在 A 股赚钱**：这个需求对实盘盈利是否有效，还是只是「看起来合理」？
   - **是否破坏系统稳定性与正确性**：会不会引入回归、破坏现有风控 / 执行 / 数据链路的正确性？
   - **是否导致过拟合**：是不是在用历史数据「背答案」，样本外 / 前瞻是否仍然成立？
   - 其他相关风险：数据口径、交易成本、前瞻可重复性、护栏等。

   评估要**对抗式**进行——主动寻找证伪它的证据，而不是找理由支持它。可借助多 agent 工作流（Workflow 工具）和 `adversarial-strategy-validation` 技能。若评估结论是「不合理」，说明原因、不要硬做。

2. **调研清楚、评估合理后，直接实现，不要问**
   可行性确认没问题后就直接开工，不用逐步征求同意。

3. **实现完备后自动收尾，全程非必要不打扰**
   实现并自测通过后，自行执行下面的「收尾流程」，非必要不要提问。

## 收尾流程

改代码前先从**最新 main** 新建 git worktree（或复用 harness 提供的 worktree，但必须基于最新 main，否则先 fetch/rebase）。实现完成或用户说「收尾」后，一口气执行、不必逐步确认：

1. `git commit`（提交信息附 `Co-Authored-By` 尾注）
2. `git push` 推送分支
3. `gh pr create`（base 为 `main`，PR body 附生成尾注）
4. `gh pr merge --squash` 合并 PR
5. 删除远程分支，回主仓 `git checkout main`
6. `git pull --ff-only` 同步合并结果，并汇报 PR 链接

## 何时才停下来问

只有以下情况才打断用户：破坏性 / 不可逆操作、真正需要用户拍板的方案分歧、或需求经评估确实不合理（应说明原因而非硬做）。除此之外一律自行推进。
